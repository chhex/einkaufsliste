server {
    listen 80;
    server_name comprarli.com www.comprarli.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name comprarli.com www.comprarli.com;

    ssl_certificate /etc/letsencrypt/live/comprarli.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/comprarli.com/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    root /usr/share/nginx/html;
    index index.html index.htm;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://einkaufsliste-backend:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
